
@{
    ViewBag.Title = "TotalDeviceByStatus";
}

@{
    var db = new ITST.Models.ITStockEntities1();
    var TotalDevicePerType = db.Devices.Where(d => d.StatusID == 1).Count();
}

<h2>TotalDeviceByStatus @ViewBag.Status</h2>

<div class="row">
    <div class="col-md-6">
        <div class="btn-group btn-group-justified">
            <a href="@Url.Action("TotalDeviceByType", "Dashboard", new { id = @ViewBag.ID })" class="btn btn-primary col-sm-3">
                Type
                <br><span class="badge badge-info"></span>
            </a>
            <a href="@Url.Action("TotalDeviceByDepartment", "Dashboard", new { id = @ViewBag.ID })" class="btn btn-primary col-sm-3">
                Department
                <br><span class="badge badge-info"></span>
            </a>
            <a href="@Url.Action("TotalDeviceByLocationStock", "Dashboard", new { id = @ViewBag.ID })" class="btn btn-primary col-sm-3">
                Location Instock
                <br><span class="badge badge-info"></span>
            </a>
        </div>
    </div>
</div>
