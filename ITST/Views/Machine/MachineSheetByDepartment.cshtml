@model IEnumerable<ITST.Models.Machine>

@{
    ViewBag.Title = "MachineSheetByDepartment";
}

<h2>PM Sheet</h2>

<h4>Department</h4>
<hr />


<table class="table table-bordered" id="list">
    <thead>
        <tr class="active">
            <th>
                @Html.DisplayNameFor(model => model.Department.DepartmentName)
            </th>
        </tr>
    </thead>
    
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a href="@Url.Action("CreateMachineSheet", "Machine", new { id = item.PlantID, did = item.DepartmentID })">@Html.DisplayFor(modelItem => item.Department.DepartmentName)</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section scripts {
    <script>
        $(function () {
            $("table#list").dataTable({
                "sPaginationType": "full_numbers"
            });
        })
    </script>
}
